const axios = require('axios'); require('dotenv').config(); async function test() { try { const apiKey = process.env.GEMINI_API_KEY; const response = await axios.post('https://generativelanguage.googleapis.com/v1beta/models/gemini-3.6-flash:generateContent?key=' + apiKey, { contents: [{ role: 'user', parts: [{ text: 'Kamu adalah Asisten AI. Pertanyaan user: arget market itu apa?' }] }] }, { validateStatus: s => s < 500 }); console.log(response.status, response.data); } catch(e) { console.error(e.message); } } test();
